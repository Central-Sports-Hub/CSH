"use client";
import { useState } from "react";

//Make for onChange
const searchInput = () => {
  const [searchQuery, setSearchQuery] = useState("");

  return <input />;
};

export default searchInput;
